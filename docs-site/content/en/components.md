---
title: Component catalog
description: Browse the full catalog, grouped by category, with a dedicated route for every component.
kicker: Components
---

Each component now has its own documentation page, with live previews using the optional runtime whenever behavior is needed.

The main catalog now covers the same **81 core components** currently shown in the Element Plus overview, plus a few native `retro8-ui` extras.

That scales much better for the library because every route can grow with:

- extra examples
- accessibility notes
- future variants
- changelogs or breaking-change notes

Start with the grouped catalog below and open the component you want to explore.
